# deps
/node_modules

# generated content
.source

# test & build
/coverage
/.next/
/out/
/build
*.tsbuildinfo

# misc
.DS_Store
*.pem
/.pnp
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# cloudflare / opennext
/.open-next/
.dev.vars
.wrangler/
# cloudflare-env.d.ts is committed (not ignored): Workers Builds' CI doesn't
# run `wrangler types`, so the generated bindings need to already be in the
# tree for the build to typecheck. Regenerate with `npm run cf-typegen`
# after any change to wrangler.jsonc's bindings.

# others
.env*.local
.vercel
next-env.d.ts
